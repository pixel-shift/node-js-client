/*
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

const models = require('./index');

/**
 * Resize image with touch-from-outside semantics where width and height
 * represent minimums. e.g. the image will
 * be no smaller that the bounding-box.
 * Aspect ratio is preserved, so image will be larger than or equal to the
 * bounding box in one dimension, and
 * exactly fit the other dimension
 *
 * @extends models['TransformUnit']
 */
class ImageResizeMin extends models['TransformUnit'] {
  /**
   * Create a ImageResizeMin.
   * @member {number} width Minimum width
   * @member {number} height Minimum height
   * @member {string} [resizeKernel] The resampling algorithm used for
   * resizing. Possible values include: 'lanczos3', 'lanczos2', 'cubic',
   * 'nearest'. Default value: 'lanczos3' .
   */
  constructor() {
    super();
  }

  /**
   * Defines the metadata of ImageResizeMin
   *
   * @returns {object} metadata of ImageResizeMin
   *
   */
  mapper() {
    return {
      required: false,
      serializedName: 'Image_ResizeMin',
      type: {
        name: 'Composite',
        polymorphicDiscriminator: {
          serializedName: 'kind',
          clientName: 'kind'
        },
        uberParent: 'TransformUnit',
        className: 'ImageResizeMin',
        modelProperties: {
          kind: {
            required: true,
            serializedName: 'kind',
            isPolymorphicDiscriminator: true,
            type: {
              name: 'String'
            }
          },
          width: {
            required: true,
            serializedName: 'width',
            type: {
              name: 'Number'
            }
          },
          height: {
            required: true,
            serializedName: 'height',
            type: {
              name: 'Number'
            }
          },
          resizeKernel: {
            required: false,
            serializedName: 'resizeKernel',
            defaultValue: 'lanczos3',
            type: {
              name: 'Enum',
              allowedValues: [ 'lanczos3', 'lanczos2', 'cubic', 'nearest' ]
            }
          }
        }
      }
    };
  }
}

module.exports = ImageResizeMin;
